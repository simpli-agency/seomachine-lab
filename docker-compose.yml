services:
  seomachine:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./projects:/app/projects
      - ./credentials:/app/credentials
      - ./.claude:/app/.claude
